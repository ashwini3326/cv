#import "@preview/modern-cv:0.8.0": *


#resume-skill-item(
  "Development",
  ("Expert in" + strong(" Python") + " and" + strong(" SQL") + " with focus on financial applications",)
)

#resume-skill-item(
  "Data Processing",
  ("Experience with" + strong(" Big Data") + "," + strong(" Data Analytics") + ", and" + strong(" Excel/VBA"),)
)

#resume-skill-item(
  "API Development",
  ("Proficient in" + strong(" REST APIs") + "," + strong(" GraphQL") + ", and Web Services",)
)

#resume-skill-item(
  "Tools & Technologies",
  ("Expertise in" + strong(" Git") + "," + strong(" Docker") + ", and Cloud Platforms",)
)

#resume-skill-item(
  "Security",
  ("Understanding of" + strong(" OAuth") + "," + strong(" JWT") + ", and API Security",)
)

#resume-skill-item(
  "Monitoring",
  ("Familiar with" + strong(" Prometheus") + "," + strong(" Grafana") + ", and logging systems",)
)

#resume-skill-item(
  "Library",
  ("Nowadays using " + strong("FastAPI, Click ") + "and" + strong(" PyTest"),)
)
#resume-skill-item(
  "orchestration",
  ("Kubernetes",),
)
#resume-skill-item(
  "Database", ( "It's been long but I have used " + strong("PostgreSQL") + " and Redis",)
  
)
#resume-skill-item(
  "Cloud Services", (strong("AWS"), "Azure", "GitLab") 
)

#resume-skill-item(
  "OS", ("Using " + strong("Fedora ") + 
  "for my regular work. " + 
    strong("RHEL ") + " for Server needs. " +
  "Keen in " + strong("FreeBSD"),)
)
